<UserControl x:Class="TradeJournal.View.EventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TradeJournal.View"
             mc:Ignorable="d" 
             x:Name="EventsWindow"
             d:DesignHeight="655" d:DesignWidth="1215">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="50*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Grid.Column="1" Background="Tan">
            <RichTextBox VerticalScrollBarVisibility="Visible"/>
        </DockPanel>
        <GridSplitter Grid.Row="1"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Center"
              Background="Black" 
              Height="5"
           />
        <DockPanel Grid.Row="2" Grid.Column="0" Background="Brown">
            <Image Source="/Images/placeholder.png"/>
        </DockPanel>

    </Grid>
</UserControl>
